{
    "configurations": [
        {
            "type": "aws-sam",
            "request": "direct-invoke",
            "name": "finance-tracker:finance-tracker",
            "invokeTarget": {
                "target": "template",
                "templatePath": "${workspaceFolder}/template.yaml",
                "logicalId": "finance-tracker"
            },
            "lambda": {
                "payload": {},
                "environmentVariables": {}
            }
        }
    ]
}